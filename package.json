{
  "private": true,
  "scripts": {
    "bootstrap": "npm i && npm run build:only && npm run link",
    "clean": "node scripts/scripts.js --clean",
    "dev": "lerna run watch --stream --parallel",
    "build": "lerna run build --stream --parallel",
    "build:only": "lerna run build:only --stream --parallel",
    "example:nestjs:react:prod": "cd example/nestjs-react-ssr && npm run prod",
    "example:nestjs:react:prod:vite": "cd example/nestjs-react-ssr && npm run prod:vite",
    "example:nestjs:react": "cd example/nestjs-react-ssr && npm start",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/algesthesiah/ssr.git"
  },
  "author": "algesthesiah",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/algesthesiah/ssr/issues"
  },
  "homepage": "https://github.com/algesthesiah/ssr#readme",
  "devDependencies": {
    "@commitlint/cli": "^16.2.3",
    "@commitlint/config-conventional": "^16.2.1",
    "chalk": "^4.1.1",
    "concurrently": "^5.3.0",
    "core-js": "^3.6.4",
    "cross-env": "^7.0.3",
    "eslint-plugin-airbnb-react": "^1.0.2",
    "husky": "^7.0.4",
    "lint-staged": "^12.3.7",
    "rimraf": "^3.0.2",
    "tsc-transpile-only": "^0.0.3",
    "typescript": "^4.6.3"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx}": "eslint --fix"
  }
}
